import Add from '@mui/icons-material/Add';
import Delete from '@mui/icons-material/Delete';
import { IconButton } from '@repro/component-library';

export default function Other() {
  return (
    <div>
      <Add />
      <Delete />
      <IconButton label="Action" />
    </div>
  );
}
